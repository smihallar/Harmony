@if (userCanUpdateMatches)
{
    @* <button @onclick="UpdateMatches">Update Matches</button> *@
    @if (userHasMatches)
    {
        // List of Match components
    }
    else
    {
        <p>No matches found.</p>
    }
}
@code {

}
